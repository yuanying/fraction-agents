# Holds the agent directory (auth.json from the login) and the host's data. No storageClassName: the environment's overlay picks it.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agent-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
